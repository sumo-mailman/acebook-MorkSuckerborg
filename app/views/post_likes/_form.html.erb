<%= form_with(model: [post, post_like], local: true) do |form| %>
  <% if post_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_like.errors.count, "error") %> prohibited this post_like from being saved:</h2>

      <ul>
        <% post_like.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :post_id, value: @post.id %>
  </div>

  <div class="field">
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= form.submit "#{pluralize(post.post_likes.count, "Like")}" %>
  </div>
<% end %>
