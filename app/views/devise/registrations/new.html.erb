<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body class="text-center">
  <div class="col d-flex justify-content-center">
    <main style="max-width: 330px; width: 100%;">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <h1 class="h3 mb-3 fw-normal">Sign up</h1>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="form-floating">
            <%= f.text_field :name, autofocus: true, autocomplete: "name", type: "name", class: "form-control", id: "floatingInput", placeholder: "name" %>
            <%= f.label :name, for: "floatingInput" %>
          </div>

          <br>
          
          <div class="form-floating">
            <%= f.email_field :email, autofocus: true, autocomplete: "email", type: "email", class: "form-control", id: "floatingInput", placeholder: "name@example.com" %>
            <%= f.label :email, for: "floatingInput" %>
          </div>

            <br>

          <div class="form-floating">
            <%= f.password_field :password, autocomplete: "new-password", type: "password", class: "form-control", id: "floatingInput", placeholder: "password" %>
            <%= f.label :password, for: "floatingPassword" %>
          </div>

          <div class="form-floating">
            <%= f.password_field :password_confirmation, autocomplete: "new-password", type: "password", class: "form-control", id: "floatingInput", placeholder: "password" %>
            <%= f.label :password_confirmation, for: "floatingPassword" %>
          </div>

          <br>

          <div class="actions">
            <%= f.submit "Sign up", class: "w-100 btn btn-lg btn-primary"%>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>


    </main>
  </div>
</body>
</html>

